<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TGSpaces" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/screen_bg</item>
        <item name="android:navigationBarColor">@color/screen_bg</item>
        <item name="colorPrimary">@color/button_primary</item>
        <item name="colorAccent">@color/button_primary</item>
    </style>

    <style name="Theme.TGSpaces" parent="Base.Theme.TGSpaces" />
</resources>
